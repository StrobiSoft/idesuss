# Idesüss daily log — 2026-09-22

## Goal
Close the Radio privacy/player round safely, improve cross-page/profile UX, diagnose live-presence counting, and make the profile/avatar flow clearer and safer.

## Decisions and progress
- Completed the server-managed Radio privacy proxy round and kept the client from accepting arbitrary upstream stream URLs.
- Added defensive Spy Trap behavior for abnormal probe paths with staged detection, warning/escalation logging, and no offensive response.
- Fixed proxy handling for custom HTTPS radio ports and HLS manifests so HLS segment/key URLs remain behind the Idesüss proxy rather than exposing provider URLs directly.
- Removed the temporary separate Node 20 CI route after review. Returned to the original Node 22 validation workflow and hardened it with read-only contents permission, disabled persisted checkout credentials, pinned GitHub Actions SHAs, timeout, and concurrency control.
- Verified the hardened original workflow with the Radio proxy/API/Spy Trap checks; CI passed.
- PR #50 passed CI and was squash-merged to main as `52a7ac484f828352d555cd974e2d8233676394a7`.
- Implemented the simplified Radio transport: one Play/Pause control, no separate Stop button, short preset tap starts playback immediately, and a translucent glass-style sticky transport card was added.
- Fixed duplicated Play/Pause glyphs caused by combining hard-coded icons with localized labels. PR #51 passed CI and was squash-merged to main as `e587ada049786257c00af4bdc48a51829541a512`.
- Fixed the profile modal after email confirmation so it opens from the visible top of the viewport and remains scrollable.
- Added explicit cross-page navigation: Home and Radio buttons in Webapp; dynamic Login/Profile navigation in Radio.
- Fixed Radio startup ordering so saved preset 1 is restored before the locale default station; a saved station no longer falls back to Rádió 1 merely because the user left and reopened Radio.
- PR #52 passed CI and was squash-merged to main.
- Investigated the online-user counter in Supabase. Two simultaneous `online_visitors` records were present, but the schema stores visitor IDs rather than IP addresses. Closing a second Idesüss browser tab reduced the live counter from 2 to 1, strongly indicating that multiple tabs can currently be counted as multiple online visitors.
- Product decision: online-user counting should deduplicate tabs within one browser profile; IP address should not be the primary identity/deduplication mechanism.
- Improved the profile avatar flow: emoji buttons are centered; a selected custom image is previewed locally; the image can be dragged/repositioned and zoomed; explicit acceptance is required; upload to moderation is deferred until Profile save.
- Improved profile-save diagnostics so reserved/taken/invalid-name and other failures are shown with a concrete reason instead of only a generic failure message.
- PR #53 passed CI and was squash-merged to main as `6f1c108e4fd1af6db0db4f036c0fc7fadb0f78be`.
- Live verification confirmed emoji alignment and reserved-name enforcement work correctly.
- Created a deliberate owner exception for nickname `StZoo`: removed it from the general reserved-name table and assigned normalized `stzoo` to the current owner test profile. Database uniqueness continues to prevent another profile from taking the same nickname.

## Deferred / follow-up
- Complete live verification of the new custom-avatar crop/accept/moderation-submit flow.
- Fix presence counting so multiple tabs from the same browser profile do not inflate the online-user number.
- Continue Radio/PWA work: Media Session/system controls and persistent-shell strategy for uninterrupted in-site navigation.
- Continue security hardening of the Radio proxy before production deployment, including remaining SSRF/DNS-rebinding, trusted-proxy, rate/concurrency, state-retention and HLS edge-case review.
- Structured Bug Report form remains queued after the current Radio/profile/PWA stabilization round.
- Social/friends work remains paused until the current Radio/auth/profile round is stable.

## Closing state
- Radio proxy/security and simplified glass-player changes are merged.
- Profile modal/cross-navigation/preset-restore fixes are merged.
- Avatar editor/error-reporting changes are merged and partially live-verified.
- Known next bug: one browser with multiple Idesüss tabs can inflate the online-user counter.
- Latest main merge from the avatar round: `6f1c108e4fd1af6db0db4f036c0fc7fadb0f78be`.

## Closed
Closed at 23:54 CEST, 2026-09-22 (Europe/Amsterdam).
