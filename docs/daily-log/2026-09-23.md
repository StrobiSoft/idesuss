# Idesüss daily log — 2026-09-23

## Goal
Finish live verification and stabilization of the profile/avatar flow, correct online-presence deduplication, complete the EULA/profile gate, and close the Radio Media Session/PWA round without reopening completed work.

## Current state
- Radio privacy proxy/security round is merged and CI-green.
- Simplified one-button Play/Pause glass player is merged and live.
- Saved preset 1 is restored before locale defaults when Radio is reopened.
- Profile modal positioning and explicit Home/Radio/Login/Profile navigation are merged.
- Avatar emoji alignment and reserved-name handling are live-verified.
- Browser-tab online-presence deduplication was already merged in PR #64; only final live multi-tab retest remains.
- Full custom-avatar upload/crop/submit path is live-verified to production storage/database.
- Avatar moderation inbox, reviewer queue, approve/reject backend and approved-avatar activation are merged in PR #72.
- Versioned EULA page plus server-enforced profile-save acceptance gate are merged in PR #73 and production Supabase.
- iPhone EULA checkbox/layout overflow and moderation badge hidden-state regression are fixed in PR #74.
- Root PWA service-worker registration, Radio PWA metadata/shortcuts and hardened Media Session metadata/artwork are merged in PR #74.
- Test account strobelzoltan@gmail.com was deleted from production auth so the registration/EULA path can be tested from a clean state.
- Password Security Gateway VM101 backend is green: install/systemd/listen/health/nginx/HIBP/Spy Trap/zero-password-logging all PASS. Public route is still blocked only on security.idesuss.net DNS/TLS ingress.
- idesuss.net registration is confirmed at Hostinger; the remaining public-gateway action is the external DNS/TLS route for security.idesuss.net.

## Work log
- 00:00 CEST — New daily log opened from the 2026-09-22 closing state.
- Presence implementation re-checked: PR #64 already contained same-browser multi-tab deduplication.
- Avatar moderation round completed and merged as PR #72.
- EULA/profile-save enforcement completed and merged as PR #73; production migration applied.
- Mobile EULA layout, moderation badge hidden state, Media Session/PWA round completed and merged as PR #74; CI green.
- Fresh NOEMI Bridge operation `idesuss.password-gateway-vm101` succeeded again; only `PUBLIC_ROUTE=BLOCKED_TLS_OR_DNS` remains.

## Remaining live verification / external dependency
- Re-register strobelzoltan@gmail.com and verify EULA checkbox blocks Profile save until accepted, then verify acceptance persists by version.
- Approve a pending custom avatar and verify the approved image becomes the active profile avatar on the profile and webapp surfaces.
- One-browser-profile 1 -> 2 -> 3 tab online-counter retest; expected count remains one.
- Configure Hostinger DNS/public ingress for `security.idesuss.net`, obtain/activate TLS on VM101, externally validate health + API + CORS, then change `PASSWORD_SECURITY_ENFORCEMENT` from `pending-vm101` to `required`.
